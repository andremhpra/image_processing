"""A small Tkinter GUI for exercising every image operation in this project by hand.

Nothing here implements an image operation itself, it only reads a
declarative list of them (see `registry.py`) and builds a generic UI around
whatever it finds, so adding a new technique anywhere in the project (single-
image or multi-image) is a registry entry away, not a GUI change.
"""
